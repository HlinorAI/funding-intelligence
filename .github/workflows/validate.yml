name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    name: Runner, schemas, and public-safety checks
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Install test dependencies
        run: python -m pip install -r tests/requirements-test.txt

      - name: Pytest regression suite
        run: python -m pytest -q

      - name: Runner regression suite
        run: python3 runtime/runner.py --check-all

      - name: Decision-quality benchmark suite
        run: python3 runtime/run_benchmarks.py

      - name: Compile Python modules
        run: python3 -m py_compile runtime/project_contract.py runtime/ingest.py runtime/runner.py runtime/verify_route.py runtime/validate_schemas.py runtime/render_report.py runtime/run_benchmarks.py runtime/health_check.py runtime/validate_workflows.py

      - name: Health-check self-test
        run: python3 runtime/health_check.py --self-test

      - name: Validate embedded GitHub Actions scripts
        run: |
          python3 runtime/validate_workflows.py --self-test
          python3 runtime/validate_workflows.py

      - name: Validate YAML, schemas, program cards, route output, issue forms, and privacy
        run: python3 runtime/validate_schemas.py

      - name: Run canonical ingestion-to-report workflow
        run: |
          python3 runtime/ingest.py examples/example-ai-startup/intake.json --type json --output /tmp/example-project.yaml
          python3 runtime/runner.py /tmp/example-project.yaml --output /tmp/example-report.yaml
          python3 runtime/verify_route.py /tmp/example-project.yaml --all-ai --evidence-dir examples/example-ai-startup/evidence --output /tmp/example-routes.yaml
          python3 runtime/render_report.py /tmp/example-report.yaml /tmp/example-routes.yaml --output /tmp/opportunity-report.md
          test -s /tmp/opportunity-report.md
          grep -q "Opportunity Report: Example AI Infrastructure Startup" /tmp/opportunity-report.md
