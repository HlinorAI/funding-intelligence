# Vertical knowledge packs

Packs extend the core knowledge layer without mixing incompatible decision rules.

Each pack contains:

- `programs/` — vertical-specific opportunities and resource routes;
- `rules/` — fit, stage and evidence rules for that vertical.

The runner auto-loads a pack only when the project's structured facts match its vertical trigger. The current first pack is `ai`.
